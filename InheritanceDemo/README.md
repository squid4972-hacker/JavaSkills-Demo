An application that handles setting up vacation plans. It will calculate take the value of a user-provided budget
and when given the cost for an A-la-carte vacation and all-inclusive vacation will then calculate if it is under or over budget
and by how much. Includes a tester file.
